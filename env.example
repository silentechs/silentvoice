# Copy to `.env.local` and fill in values (do not commit real secrets)

# -----------------------------
# Database
# -----------------------------
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DB_NAME?sslmode=require"

# -----------------------------
# App
# -----------------------------
NEXT_PUBLIC_APP_URL="http://localhost:3000"
ADMIN_EMAIL="mubarick391@gmail.com"
MODERATION_SECRET="change-me-to-a-long-random-secret"

# -----------------------------
# Email (Resend)
# -----------------------------
RESEND_API_KEY="re_your_api_key_here"
# Must be a verified sending identity in Resend (email/domain).
# Either RESEND_FROM or FROM_EMAIL can be used.
RESEND_FROM="Silent Voice Sanctuary <onboarding@resend.dev>"
FROM_EMAIL="Silent Voice Sanctuary <onboarding@resend.dev>"

# -----------------------------
# Cloudflare R2 (uploads)
# -----------------------------
R2_ACCOUNT_ID=""
R2_ACCESS_KEY_ID=""
R2_SECRET_ACCESS_KEY=""
R2_BUCKET_NAME=""
# Public base URL for serving objects (either of these can be used)
R2_PUBLIC_URL=""
R2_PUBLIC_DOMAIN=""

